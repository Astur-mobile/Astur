/**
 * Ensures `appPath` holds a current extraction of `zipPath`, and returns
 * `appPath`. See ensure-ios-app.mjs for the freshness rules.
 */
export function ensureIosApp(appPath: string, zipPath: string): string;
