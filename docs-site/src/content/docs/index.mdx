---
title: Astur Docs
description: Device-native mobile automation with Playwright ergonomics.
template: splash
hero:
  title: Astur Docs
  tagline: Fast, native Android and iOS automation without an Appium server.
  actions:
    - text: Get Started
      link: ./getting-started/
      icon: right-arrow
    - text: Open Inspector
      link: ./inspector/
      variant: secondary
---

<section class="landing-shell" aria-label="Astur overview">
  <div class="landing-shell-copy">
    <p class="landing-eyebrow">Native mobile runtime</p>
    <h2>Inspect, author, and ship mobile tests in one fast loop.</h2>
    <div class="landing-copy-text">
      Astur keeps the authoring surface tight: stream the device, inspect the semantic tree,
      record the flow, and export reusable <span class="landing-inline-code">@astur/test</span> code without adding an Appium or WebDriver layer.
    </div>
    <div class="landing-chip-row">
      <span>Android emulators + devices</span>
      <span>iOS simulators</span>
      <span>Live inspector + codegen</span>
      <span>Playwright-style API</span>
    </div>
  </div>

  <div class="landing-console" aria-label="Astur authoring preview">
    <div class="landing-console-bar">
      <span>runtime</span>
      <span>authoring preview</span>
    </div>
    <pre><code>{`$ npx astur-mobile codegen --android --device emulator-5554

await device.getByLabel('Email').fill('qa@example.com');
await device.getByRole('button', { name: 'Login' }).tap();
await expect(device.getByText('Welcome')).toBeVisible();`}</code></pre>
  </div>
</section>

<section class="landing-feature-grid" aria-label="Astur capabilities">
  <article class="landing-feature-card">
    <span class="landing-feature-index">01</span>
    <h3>Playwright-style authoring</h3>
    <p>Use fixtures, locators, assertions, retries, and reports that already feel natural to Playwright users.</p>
  </article>
  <article class="landing-feature-card">
    <span class="landing-feature-index">02</span>
    <h3>Native platform agents</h3>
    <p>Android runs through UIAutomator and iOS through XCUITest, keeping control close to the platform UI stack.</p>
  </article>
  <article class="landing-feature-card">
    <span class="landing-feature-index">03</span>
    <h3>Live inspector and codegen</h3>
    <p>Mirror the device, rank semantic locators, record actions, and export starter flows without leaving the docs workflow.</p>
  </article>
  <article class="landing-feature-card">
    <span class="landing-feature-index">04</span>
    <h3>One repository, one system</h3>
    <p>Drivers, agents, test API, CLI, examples, and docs evolve together instead of splitting across disconnected tooling.</p>
  </article>
</section>

Astur keeps tests small while the runtime and native agents absorb device selection, waits, gestures, screenshots, diagnostics, and inspector state.

## Why Astur

Astur is named after the astrolabe: a portable instrument that compressed complex observation and calculation into something small enough to hold. Astur brings that same idea to handheld-device automation: a compact test API backed by a fast native runtime.

```ts
import { expect, test } from '@astur/test';

test('login', async ({ device }) => {
  await device.getByLabel('Email').fill('qa@example.com');
  await device.getByLabel('Password').fill('secret');
  await device.getByRole('button', { name: 'Login' }).tap();

  await expect(device.getByText('Welcome')).toBeVisible();
});
```

Start with [Getting Started](./getting-started/) or open [Inspector And Codegen](./inspector/) if you want to generate your first test from a running device.
